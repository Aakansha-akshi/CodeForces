n, m = map(int, input().split())
color = False
for _ in range(n):
    pixels = input().split()
    for pixel in pixels:
        if pixel == 'C' or pixel == 'M' or pixel == 'Y':
            color = True
if color:
    print("#Color")
else:
    print("#Black&White")
